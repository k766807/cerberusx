{
  "name": "cerberusx",
  "version": "1.1.0",
  "private": true,
  "description": "CerberusX: a polished Render-ready three-layer spacecraft autonomy safety demo.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "node scripts/build-check.js",
    "migrate": "node migrate.js",
    "check": "node --check server.js && node --check migrate.js && node scripts/build-check.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
